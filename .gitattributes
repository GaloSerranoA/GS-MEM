# Sovereign embedding model weights — tracked via Git LFS.
# (~86 MB MiniLM-L6-v2 weights + tokenizer; keep them out of the git object DB.)
models/** filter=lfs diff=lfs merge=lfs -text
*.sovereign filter=lfs diff=lfs merge=lfs -text
*.sovereign-tokenizer filter=lfs diff=lfs merge=lfs -text
